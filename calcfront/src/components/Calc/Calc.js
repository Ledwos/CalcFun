import React from 'react';
import Keypad from '../Keypad/Keypad';

const Calc = () => {
    return (
        <div>
            <p>I'm the calc</p>
            <Keypad />
        </div>
    );
};

export default Calc;
